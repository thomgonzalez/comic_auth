version: "3.5"

services:
  db:
    image: postgres
    env_file: ".db.env"
    container_name: comic_postgres
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
  mongo:
    image: mongo:latest
    env_file: ".db.env"
    container_name: comic_mongo
    restart: always
    environment: 
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=root
      - MONGO_INITDB_DATABASE=mongo_db
    volumes:
      - ./mongo_data:/data/db
      - ./mongo_data/docker/_mongo/fixtures:/import
      - ./mongo_data/docker/_mongo/scripts/init.sh:/docker-entrypoint-initdb.d/setup.sh
    ports:
      - 27017:27017
    links:
      - mongodb
  backend:
    build:
      context: .
      dockerfile: docker/back/Dockerfile
    container_name: comic_backend
    env_file:
      - ".db.env"
      - ".back.env"
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
volumes:
  postgres_data:
  mongo_data: